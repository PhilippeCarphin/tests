get_filename_component(dir ${CMAKE_CURRENT_SOURCE_DIR} NAME)
message(STATUS "[1;32mEntering directory ${dir}[0m")
if(DEFINED subdir_SHARED_LIBS)
    set(BUILD_SHARED_LIBS "${subdir_SHARED_LIBS}")
endif()

cmake_print_variables(BUILD_SHARED_LIBS)
set(subdir_var "subdir_var_value")
cmake_print_variables(subdir_var)
cmake_print_variables(parent_var)
list(APPEND parent_list D E F)
cmake_print_variables(parent_list)
set(parent_list ${parent_list} PARENT_SCOPE)
set(parent_var "parent_var_value_from_subdir" PARENT_SCOPE)
message(STATUS "[1;32mLeaving directory ${dir}[0m")
